# Goal 5 Training Layer Journal

## Checkpoint: Dedicated Worktree Migration

- Worktree: `/Users/zacharywright/Documents/GitHub/hermes_dev/.codex-goal-worktrees/hermes-goal-5-training`
- Branch: `codex/hermes-goal-5-training-20260702-1027`
- Remote: `origin https://github.com/zwright8/hermes-flight-recorder.git`
- Initial status: clean
- Missing at migration time: `docs/agentic-finetune-24-7-goals.md`,
  `docs/agentic-finetune-autonomous-operations.md`,
  `docs/agentic-finetune-autonomous-goals.md`, and
  `experiments/autonomy/supervisor_state.json`

No shared-checkout changes were staged, reverted, or copied into this worktree.

## Checkpoint: Registry-Backed Agentic Training Plans

### Objective

Create the first clean Goal 5 packet in the dedicated worktree: a reproducible,
side-effect-free planner for SFT, action SFT, DPO, SFT-then-DPO,
reward/process-reward, GRPO, and RL paths from registered model and dataset
manifests.

### Work Completed

- Added `flightrecorder.agentic_training_plan` for dependency-free dry-run
  planning.
- Added `scripts/plan_agentic_training.py`.
- Added `hfr.agentic_training_plan.v1` schema and catalog registration.
- Locked behavior with focused tests for ready plans, unredacted dataset
  blocking, future-RL gating, CLI output, and schema registration.
- Added Training Layer docs and bootstrapped dedicated-worktree autonomy state.

### Verification Run

- `python3 -m unittest tests.test_agentic_training_plan tests.test_schema_registry`: pass, 21 tests.
- `python3 -m py_compile flightrecorder/agentic_training_plan.py scripts/plan_agentic_training.py tests/test_agentic_training_plan.py`: pass.
- `python3 -m unittest tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: pass.
- `python3 scripts/plan_agentic_training.py --mode sft_then_dpo ... --out /tmp/hfr-agentic-training-plan/plan.json`: pass.
- `python3 -m flightrecorder schemas --check /tmp/hfr-agentic-training-plan/plan.json`: pass.
- `python3 -m flightrecorder schemas --name agentic_training_plan --write-dir /tmp/hfr-agentic-training-schema --force`: pass.
- `python3 -m json.tool experiments/autonomy/supervisor_state.json`: pass.
- `python3 -m json.tool flightrecorder/schemas/agentic_training_plan.v1.schema.json`: pass.
- `git diff --check`: pass.

### Git Persistence

- Branch: `codex/hermes-goal-5-training-20260702-1027`
- Implementation commit SHA: `59ddc118f17c87e2f5da352a39d408ddc4564490`
- Remote branch verified: `origin/codex/hermes-goal-5-training-20260702-1027`
- PR URL: `https://github.com/zwright8/hermes-flight-recorder/pull/4`

### Risks

- Plans are dry-run handoffs only. They do not prove local trainer runtime
  dependencies are installed.
- Real GRPO/RL remains a future extension point and is blocked unless explicitly
  enabled for planning.
- The dedicated worktree did not include the broader autonomous-goal docs, so
  Goal 5 state was bootstrapped here.

### Next Recommended Packet

Generate durable sample plan artifacts from registered local manifests and
connect them to trainer preflight/archive checks, still without launching
trainers.

## Checkpoint: Schema-Checked Training Plan Artifacts

### Objective

Publish durable registered-input examples and connect ready agentic training
plans to the existing trainer preflight/archive handoff path without launching
trainers.

### Work Completed

- Added `--created-at` to `scripts/plan_agentic_training.py` for reproducible
  sample artifacts.
- Added `trainer-preflight --agentic-training-plan` support.
- Preflight now fingerprints `hfr.agentic_training_plan.v1` artifacts,
  schema-checks them, and requires `ready_for_external_trainer_plan`.
- Trainer archive and consumer-plan flows now inherit the agentic plan as a
  trainer input through the existing artifact-copy path.
- Added `examples/agentic_training/` with synthetic registered model/dataset
  manifests, schema-conforming trainer views, and a committed SFT-then-DPO plan.
- Updated Training docs and focused regression coverage.

### Verification Run

- `python3 -m unittest tests.test_agentic_training_plan tests.test_trainer_preflight tests.test_schema_registry`: pass, 38 tests.
- `python3 -m py_compile flightrecorder/agentic_training_plan.py flightrecorder/preflight.py flightrecorder/cli.py scripts/plan_agentic_training.py tests/test_agentic_training_plan.py tests/test_trainer_preflight.py`: pass.
- `python3 -m flightrecorder schemas --check examples/agentic_training/plans/sft_then_dpo_plan.json`: pass.
- `python3 -m flightrecorder schemas --check-jsonl` for the example SFT, action SFT, DPO, reward-model, step-reward, and episode views: pass.
- Regenerating `examples/agentic_training/plans/sft_then_dpo_plan.json` with fixed `--created-at` and checking `git diff --exit-code` for that file: pass.

### Git Persistence

- Branch: `codex/hermes-goal-5-training-20260702-1027`
- Prior implementation commit SHA: `59ddc118f17c87e2f5da352a39d408ddc4564490`
- This packet commit SHA: `892f7fe4116ca11bc634d8522f2c05062bf848a6`
- PR URL: `https://github.com/zwright8/hermes-flight-recorder/pull/4`

### Risks

- The bridge proves plan handoff and archive readiness, not trainer dependency
  installation or weight updates.
- The sample dataset is synthetic and intentionally tiny; it is suitable for
  contract checks, not model-quality claims.

### Next Recommended Packet

Resolve or document the bounded tiny smoke trainer runtime path, then run only
dependency/preflight checks until registered inputs, license status, redaction
gates, and no-download/no-expensive-job constraints pass.

## Checkpoint: Tiny Smoke Runtime Preflight

### Objective

Resolve the bounded tiny-smoke runtime dependency path without launching a
trainer, importing trainer stacks, downloading model weights, or training on
unredacted traces.

### Protocol Update

- Fetched `origin` and rebased
  `codex/hermes-goal-5-training-20260702-1027` onto current `origin/main`
  before continuing this packet.
- The shared autonomous docs are now present in the dedicated worktree.
- Delivery now requires verified branch commits to land on `origin/main` under
  `/tmp/hermes-flight-recorder-main-push.lock`, then the temporary PR/branch
  surfaces must be retired.

### Work Completed

- Added `flightrecorder.agentic_training_runtime` for side-effect-free runtime
  readiness checks over ready agentic training plans.
- Added `scripts/preflight_agentic_training_runtime.py`.
- Added `hfr.agentic_training_runtime_preflight.v1` schema and catalog
  registration.
- Runtime preflight validates selected trainer-view JSONL files, checks
  manifest row counts, fingerprints plan/view files, and probes Python module
  discoverability with `importlib.util.find_spec`.
- Added tests for ready runtime preflight, missing dependency blocking, missing
  selected view blocking, CLI output, and schema registration.
- Updated Training Layer docs, the top-level training pipeline, the example
  README, supervisor state, and packaging manifest.

### Verification Run

- `python3 -m unittest tests.test_agentic_training_runtime tests.test_agentic_training_plan tests.test_trainer_preflight tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: pass, 28 tests.
- `python3 -m py_compile flightrecorder/agentic_training_runtime.py scripts/preflight_agentic_training_runtime.py tests/test_agentic_training_runtime.py flightrecorder/agentic_training_plan.py scripts/plan_agentic_training.py tests/test_agentic_training_plan.py`: pass.
- Ready runtime preflight with `--skip-default-modules --require-module json`: pass, emitted `ready_for_tiny_smoke_launch`.
- Blocked runtime preflight with `--require-module hfr_missing_dependency_for_test`: pass, exited 1 and emitted `block_tiny_smoke_launch`.
- `python3 -m flightrecorder schemas --check` for ready and blocked runtime-preflight artifacts: pass.
- `python3 -m flightrecorder schemas --name agentic_training_runtime_preflight --write-dir /tmp/hfr-agentic-runtime-preflight/schema --force`: pass.
- `python3 -m json.tool flightrecorder/schemas/agentic_training_runtime_preflight.v1.schema.json`: pass.
- `python3 -m json.tool flightrecorder/schemas/manifest.json`: pass.
- `python3 -m json.tool experiments/autonomy/supervisor_state.json`: pass.
- `git diff --check`: pass.

`python3 -m flightrecorder schemas --check
experiments/autonomy/supervisor_state.json` cannot run in this branch because
`hfr.autonomy.supervisor_state.v1` is not registered in the bundled schema
catalog.

### Git Persistence

- Branch: `codex/hermes-goal-5-training-20260702-1027`
- PR URL: `https://github.com/zwright8/hermes-flight-recorder/pull/4`
- Packet commit SHA: `a37b7000b88643d8aaf6f4af436e69977bb321e7`
- Remote branch verified:
  `origin/codex/hermes-goal-5-training-20260702-1027` at
  `a37b7000b88643d8aaf6f4af436e69977bb321e7`
- Current `origin/main` at attempted integration:
  `b671b9671fff7e891fa0ee58a69f64e818694b11`
- Main SHA: blocked before push; `main` was not modified by this worker.
- PR closure / remote branch deletion: deferred because main integration is
  blocked.

### Main Integration Blocker

The main-push lock was acquired, then the branch fetched current `origin` and
attempted to rebase onto `origin/main`. Rebase hit non-trivial add/add
conflicts in:

- `experiments/autonomy/journal/2026-07-02.md`
- `experiments/autonomy/supervisor_state.json`

The conflicting commit was from the earlier Goal 5 packet history being replayed
onto a main branch that now has its own autonomy state/journal versions. Per the
updated delivery protocol, the rebase was aborted and no push to `origin/main`
was attempted. Supervisor resolution is needed to integrate the verified
runtime-preflight branch with current autonomous state.

### Main Integration Retry

On 2026-07-02T15:18:43Z the worker reacquired the main-push lock and retried
against newer `origin/main` at
`53357ff5c838c894ec1eca8515f6601dbfee4e8c`. The same non-trivial add/add
conflicts recurred in:

- `experiments/autonomy/journal/2026-07-02.md`
- `experiments/autonomy/supervisor_state.json`

The rebase was aborted again and no push to `origin/main` was attempted. This
is the second consecutive Goal 5 continuation turn with the same main
integration conflict; the branch remains pushed for supervisor resolution.

### Risks

- Runtime preflight proves schema and dependency discoverability only; it does
  not prove trainer version compatibility or execute a tiny smoke job.
- Default backend dependency checks may block until the external runner
  environment installs Axolotl, LLaMA Factory, Unsloth, or process-reward
  packages.
- No model downloads, trainer imports, weight updates, or expensive jobs were
  launched.
- Supervisor state is JSON-valid, but not schema-checkable until the repository
  adds a bundled `hfr.autonomy.supervisor_state.v1` schema.

### Next Recommended Packet

Define the training result archive and classified failure contract that
external tiny-smoke or full trainers must return before adapters/checkpoints can
be registered.
