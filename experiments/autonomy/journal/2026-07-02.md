# 2026-07-02

## Objective

Goal 3 Data Layer: make training exports selectable as registered, redacted,
versioned, split-safe datasets with auditable lineage and label provenance.

## Work Completed

- Added stable `dataset_version` values to `export-rl` manifests.
- Added `dataset_registry.json` entries that fingerprint the completed
  manifest and record source hashes, split metadata, redaction status, quality
  flags, label provenance, and trainer-view counts.
- Added redaction scanning that blocks export registration when trainer-facing
  artifacts contain unredacted secret-like assignments.
- Added label provenance to SFT, DPO, preference, and reward-model views.
- Excluded final-answer-only positive success claims from SFT, DPO chosen rows,
  and positive reward-model rows.
- Extended strict validation to recompute redaction status, label provenance,
  manifest hashes, registry lineage, and trainer-view eligibility.
- Published a bundled `dataset_registry` schema and updated related schemas.
- Updated training data docs and README handoff notes.

## Files Changed

- `flightrecorder/training.py`
- `flightrecorder/validation.py`
- `flightrecorder/schemas/manifest.json`
- `flightrecorder/schemas/dataset_registry.v1.schema.json`
- `flightrecorder/schemas/training_manifest.v1.schema.json`
- `flightrecorder/schemas/rl_dataset_metrics.v1.schema.json`
- `flightrecorder/schemas/rl_preference.v1.schema.json`
- `flightrecorder/schemas/rl_sft.v1.schema.json`
- `flightrecorder/schemas/rl_dpo.v1.schema.json`
- `flightrecorder/schemas/rl_reward_model.v1.schema.json`
- `tests/test_training_export.py`
- `TRAINING_PIPELINE.md`
- `README.md`
- `experiments/autonomy/supervisor_state.json`

## Artifacts Produced

- New export artifact: `runs/training_export/dataset_registry.json`
- New schema: `flightrecorder/schemas/dataset_registry.v1.schema.json`
- New manifest fields: `dataset_version`, `redaction_status`,
  `label_provenance`, and `registry`

## Verification Run

- `python3 -m unittest tests.test_training_export`: passed
- `python3 -m unittest tests.test_schema_registry`: passed
- `python3 -m unittest tests.test_training_gate tests.test_trainer_preflight`: passed
- `python3 -m compileall flightrecorder`: passed
- Deterministic temp `run-suite --export-rl`: passed
- `flightrecorder validate --training-export <temp>/training_export --strict`: passed
- Schema checks for manifest, dataset registry, dataset metrics, SFT, DPO, and
  reward-model artifacts: passed
- Bundled runtime `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest discover`: passed, 328 tests

## Failures Or Blockers

- No blockers. `python` and `pytest` were not available in the system
  environment, so verification used `python3 -m unittest`.

## Next Recommended Packet

Add a small registry selection or trainer-preflight requirement so external
training launchers can require an exact `dataset_version` before consuming a
manifest. After that, extend data-layer split policy with explicit held-out
scenario-id exclusion for benchmark packs.

## Checkpoint: Goal 5 Training Layer Packets

### Objective

Advance reproducible Training-layer paths without launching expensive jobs:
registry-backed dry-run plans, blocked/failed result archives, local registry
events, and schema contracts for trainer artifacts.

### Work Completed

- Extended `scripts/train_agentic_lora.py` with `--model-manifest`,
  `--dataset-manifest`, `--require-registered-inputs`, and `--result-registry`.
- Added pre-import plan checks for model license status, compatibility metadata,
  dataset redaction, passed training gate, dataset version identity,
  family-exclusive splits, quality flags, source fingerprints, required data
  files, row availability, and hyperparameters.
- Added executable `fr_action_sft` mode and dry-run extension points for
  `fr_reward_model` and `fr_step_rewards`.
- Made non-dry-run launches require registered inputs by default and write
  blocked result archives before heavy ML imports when the plan fails.
- Added local JSONL registry events for blocked, failed, and successful
  non-dry-run trainer outcomes.
- Added bundled JSON Schema contracts for `agentic_lora_training_plan`,
  `agentic_lora_training_result`, and
  `agentic_lora_training_registry_event`.
- Repaired `experiments/autonomy/supervisor_state.json` into the required
  schema shape while preserving the current Data and Harness packet records.

### Files Changed

- `scripts/train_agentic_lora.py`
- `tests/test_agentic_lora_training_plan.py`
- `docs/agentic-finetune-training-layer.md`
- `flightrecorder/schemas/agentic_lora_training_plan.v1.schema.json`
- `flightrecorder/schemas/agentic_lora_training_result.v1.schema.json`
- `flightrecorder/schemas/agentic_lora_training_registry_event.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `/tmp/hfr-train-schema-plan/trace_sft_plan.json`
- `/tmp/hfr-train-schema-blocked/trace_sft_result.json`
- `/tmp/hfr-train-schema-registry/events.jsonl`

### Verification Run

- `python3 -m unittest tests.test_agentic_lora_training_plan`: passed, 5 tests.
- `python3 -m py_compile scripts/train_agentic_lora.py tests/test_agentic_lora_training_plan.py`: passed.
- `python3 scripts/train_agentic_lora.py --mode trace_sft --dry-run --experiment-dir experiments/qwen3_4b_flightrecorder --output-dir /tmp/hfr-train-plan-trace-final2 --disable-trackio`: passed.
- `python3 scripts/train_agentic_lora.py --mode fr_sft_dpo --dry-run --experiment-dir experiments/qwen3_4b_flightrecorder --output-dir /tmp/hfr-train-plan-fr-final2 --disable-trackio`: passed.
- `python3 scripts/train_agentic_lora.py --mode trace_sft --experiment-dir experiments/qwen3_4b_flightrecorder --output-dir /tmp/hfr-train-schema-blocked --limit 1 --result-registry /tmp/hfr-train-schema-registry/events.jsonl --disable-trackio`: exited 1 as expected with a classified blocked result and registry event.
- `python3.11 -m flightrecorder schemas --check /tmp/hfr-train-schema-plan/trace_sft_plan.json`: passed.
- `python3.11 -m flightrecorder schemas --check /tmp/hfr-train-schema-blocked/trace_sft_result.json`: passed.
- `python3.11 -m flightrecorder schemas --check-jsonl /tmp/hfr-train-schema-registry/events.jsonl --name agentic_lora_training_registry_event`: passed.
- `python3.11 -m flightrecorder schemas --check experiments/autonomy/supervisor_state.json`: passed.

### Failures Or Blockers

- No expensive training or model-download job was launched.
- Broader `python3 -m unittest tests.test_trainer_preflight` currently fails
  before the trainer script path on existing export-layer issues: unredacted
  secret-like fixture values and missing `_label_provenance_summary` in
  `flightrecorder/training.py`.

### Next Recommended Packet

Add a tiny registry-backed smoke fixture path that exercises row limits,
registered manifests, result archives, and registry-event handoff without model
downloads or GPU work.

## Checkpoint: Harness Manifest Mock Runner

### Objective

Advance Goal 2 by making later Eval and Evidence workers able to run a scenario
through a common harness without live credentials or hand-edited config.

### Work Completed

- Added manifest-backed `run_scenario`, `run_suite`, `probe_model`, and
  `replay_trace` interfaces to `scripts/hermes_harness.py`.
- Added a deterministic mock runner that creates isolated sandbox paths,
  writes fake-secret canaries, captures effective tool policy, emits observer
  JSONL, scores through normal Flight Recorder artifacts, and records replay
  metadata.
- Added bundled schemas for `hfr.harness_run_manifest.v1` and
  `hfr.harness_run_result.v1`.
- Added a bundled schema for `hfr.harness_replay_result.v1`.
- Added `flightrecorder validate --harness-manifest`, `--harness-result`, and
  `--harness-replay-result`.
- Fixed harness CLI exit semantics so failed scenario scorecards, failed suites,
  and failed replays return nonzero.
- Documented the mock harness, probe, validation, and replay command flow.

### Files Changed

- `scripts/hermes_harness.py`
- `flightrecorder/cli.py`
- `flightrecorder/validation.py`
- `flightrecorder/schemas/manifest.json`
- `flightrecorder/schemas/harness_run_manifest.v1.schema.json`
- `flightrecorder/schemas/harness_run_result.v1.schema.json`
- `flightrecorder/schemas/harness_replay_result.v1.schema.json`
- `tests/test_deployment_hardening.py`
- `tests/test_schema_registry.py`
- `README.md`
- `experiments/autonomy/supervisor_state.json`
- `.omx/context/harness-layer-20260702T133525Z.md`
- `.omx/plans/prd-harness-layer.md`
- `.omx/plans/test-spec-harness-layer.md`

### Artifacts Produced

- `harness_manifest.json` and `harness_result.json` are now generated by the
  mock harness runner.
- `harness_replay_result.json` is generated by `replay-trace` and validated by
  bundled schema/CLI checks.
- `experiments/autonomy/supervisor_state.json` records the Harness checkpoint.

### Verification Run

- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest tests.test_deployment_hardening tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: passed, 16 tests.
- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/hermes_harness.py flightrecorder/cli.py flightrecorder/validation.py tests/test_deployment_hardening.py tests/test_schema_registry.py`: passed.
- Direct CLI smoke with `scripts/hermes_harness.py run-scenario`, `flightrecorder validate --run`, `flightrecorder validate --harness-manifest --harness-result`, `scripts/hermes_harness.py replay-trace`, `flightrecorder validate --harness-replay-result`, and `scripts/hermes_harness.py probe-model`: passed.
- Architect review initially blocked on exit-code and replay-schema gaps; both
  were fixed and regression tested.
- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m flightrecorder schemas --check experiments/autonomy/supervisor_state.json`: passed.

### Failures Or Blockers

- Full `tests.test_schema_registry` still has unrelated training-export
  redaction failures in the current dirty worktree.
- Live Hermes/OpenClaw/Codex-style paths still need to emit or consume the new
  common harness manifest/result contract.

### Next Recommended Packet

Bridge the existing live Hermes, OpenClaw, and Codex-style smoke/eval paths into
the common harness manifest/result contract while preserving their current CLIs.

## Checkpoint: Compare And Reviewed Gate Schemas

### Objective

Close the remaining Evidence gate-contract gap by making compare-export and
human-reviewed export gate outputs first-class, schema-checkable handoff
artifacts alongside suite and training gates.

### Work Completed

- Added public `hfr.compare_gate.v1` and `hfr.reviewed_gate.v1` JSON Schema
  contracts that enforce the shared gate decision fields:
  `readiness`, `recommendation`, `failed_checks`, and `next_actions`.
- Registered both schema contracts in the bundled schema manifest.
- Extended compare and reviewed gate regression tests so generated pass and
  block artifacts validate through `flightrecorder schemas --check`.
- Updated the training handoff docs to list all four schema-checkable gate
  result artifacts: suite, training, compare, and reviewed.
- Preserved the concurrent serving/demo current packet while recording this
  Evidence packet as completed in supervisor state.

### Files Changed

- `flightrecorder/schemas/compare_gate.v1.schema.json`
- `flightrecorder/schemas/reviewed_gate.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_compare_gate.py`
- `tests/test_reviewed_gate.py`
- `tests/test_schema_registry.py`
- `TRAINING_PIPELINE.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `.venv/bin/python -m unittest tests.test_schema_registry tests.test_compare_gate tests.test_reviewed_gate`: passed, 34 tests.
- `tmpdir=$(mktemp -d); .venv/bin/python -m flightrecorder schemas --name compare_gate --name reviewed_gate --write-dir "$tmpdir"`: passed and exported both schemas plus a manifest.
- `.venv/bin/python -m flightrecorder schemas --check experiments/autonomy/supervisor_state.json`: passed.

### Failures Or Blockers

- No expensive training, model promotion, or long-running serving process was
  launched.
- The CLI rejected an attempted `schemas --check` against the schema document
  itself because schema documents do not carry runtime artifact
  `schema_version`; the correct registry export command above passed.

### Next Recommended Packet

Follow the current supervisor queue: either capture real serving endpoint
profiles if managed endpoints are available, or first add the managed local
server lifecycle helper. The harness live-provider manifest bridge remains the
next high-priority dependency-order packet.

## Checkpoint: Managed Serving Lifecycle

### Objective

Close the Serving/demo lifecycle gap so local baseline or adapter endpoints can
be started, preflighted, consumed by Eval, logged, and terminated without
leaving unmanaged processes behind.

### Work Completed

- Reused the existing `scripts/run_managed_serving_eval.py` helper as the
  canonical lifecycle path rather than adding a duplicate wrapper.
- Added a public `hfr.serving_lifecycle_run.v1` schema and registered it as
  `serving_lifecycle_run`.
- Extended serving/demo tests so the lifecycle artifact produced by the managed
  mock server validates through the schema registry.
- Documented the lifecycle artifact schema-check command in
  `docs/serving-demo.md`.
- Ran a direct managed mock lifecycle smoke under `experiments/autonomy`, which
  wrote serving profile/check/compatibility artifacts and confirmed cleanup.

### Files Changed

- `flightrecorder/schemas/serving_lifecycle_run.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_serving_demo.py`
- `tests/test_schema_registry.py`
- `docs/serving-demo.md`
- `experiments/autonomy/serving_lifecycle_mock_20260702T140229Z/serving_lifecycle_run.json`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `.venv/bin/python -m unittest tests.test_serving_demo tests.test_schema_registry`: passed, 24 tests.
- `.venv/bin/python -m py_compile scripts/run_managed_serving_eval.py scripts/mock_openai_serving.py scripts/check_openai_serving.py tests/test_serving_demo.py`: passed.
- `tmpdir=$(mktemp -d); .venv/bin/python -m flightrecorder schemas --name serving_lifecycle_run --write-dir "$tmpdir"`: passed.
- `.venv/bin/python scripts/run_managed_serving_eval.py --server-command ".venv/bin/python scripts/mock_openai_serving.py --port <free-port> --model hfr-managed-model" --base-url http://127.0.0.1:<free-port>/v1 --model hfr-managed-model --out experiments/autonomy/serving_lifecycle_mock_20260702T140229Z --readiness-timeout 10 --probe-interval 0.2 --request-timeout 2 --require-tool-call --require-structured-output`: passed.
- `.venv/bin/python -m flightrecorder schemas --check experiments/autonomy/serving_lifecycle_mock_20260702T140229Z/serving_lifecycle_run.json`: passed.
- `pgrep -fl 'mock_openai_serving.py|serve_transformers_openai.py|run_managed_serving_eval.py' || true`: passed with no matching processes.
- `.venv/bin/python -m flightrecorder schemas --check experiments/autonomy/supervisor_state.json`: passed.

### Failures Or Blockers

- Real baseline, trace-only, and Flight Recorder endpoint profile capture still
  requires actual local endpoints or approved launch inputs. No model download,
  expensive training, promotion, or unmanaged long-running process was started.

### Next Recommended Packet

Follow dependency order with the supervisor queue: `harness_live_provider_manifest_bridge`
or `model_compatibility_probe_reports` are high-priority ready packets; real
serving endpoint profile capture should wait until endpoint launch inputs are
available through the managed lifecycle path.

## Checkpoint: Serving Endpoint Preflight And Replay Demo

### Objective

Advance Goal 6 by making OpenAI-compatible endpoints preflight-checkable before Eval and making base-vs-candidate behavior inspectable through replayable artifacts.

### Work Completed

- Added `scripts/check_openai_serving.py`, a dependency-free endpoint preflight that writes `serving_profile.json`, `compatibility_report.json`, and `serving_check.json`.
- Added managed mock endpoint mode for local smoke checks; the script starts and shuts down the mock server itself.
- Captured model identity, adapter identity, health, model list, model metadata, chat completion, tool-call compatibility, structured-output compatibility, and vLLM/SGLang readiness profile metadata.
- Added `--serving-profile` to `scripts/evaluate_hermes_heldout.py`; Eval now rejects unchecked, not-ready, wrong-base-url, or wrong-model profiles before running.
- Added `scripts/build_serving_demo_report.py` to generate `hfr.serving_demo_run.v1` JSON and Markdown replay reports from eval summaries or suite summaries.
- Updated the experiment bundle next commands and added `docs/serving-demo.md`.
- Generated a managed-mock serving preflight artifact set and a replayable demo report from existing baseline, trace-only, and Flight Recorder eval summaries.
- Terminated a pre-existing unmanaged local `serve_transformers_openai.py` process on port 8002 and verified no matching serving/eval helper processes remained.

### Files Changed

- `scripts/check_openai_serving.py`
- `scripts/build_serving_demo_report.py`
- `scripts/evaluate_hermes_heldout.py`
- `scripts/build_agentic_finetune_experiment.py`
- `tests/test_serving_demo.py`
- `docs/serving-demo.md`
- `experiments/qwen3_4b_flightrecorder/serving/mock_openai_check/serving_profile.json`
- `experiments/qwen3_4b_flightrecorder/serving/mock_openai_check/compatibility_report.json`
- `experiments/qwen3_4b_flightrecorder/serving/mock_openai_check/serving_check.json`
- `experiments/qwen3_4b_flightrecorder/serving/mock_openai_check/mock_requests.json`
- `experiments/qwen3_4b_flightrecorder/serving/demo_run.json`
- `experiments/qwen3_4b_flightrecorder/serving/DEMO_REPORT.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `experiments/qwen3_4b_flightrecorder/serving/mock_openai_check/serving_profile.json`
- `experiments/qwen3_4b_flightrecorder/serving/mock_openai_check/compatibility_report.json`
- `experiments/qwen3_4b_flightrecorder/serving/mock_openai_check/serving_check.json`
- `experiments/qwen3_4b_flightrecorder/serving/demo_run.json`
- `experiments/qwen3_4b_flightrecorder/serving/DEMO_REPORT.md`

### Verification Run

- `uv run python -m unittest tests.test_serving_demo`: pass, 5 tests.
- `uv run python -m py_compile scripts/check_openai_serving.py scripts/build_serving_demo_report.py scripts/evaluate_hermes_heldout.py scripts/build_agentic_finetune_experiment.py`: pass.
- `uv run python scripts/check_openai_serving.py --mock-response 'hfr serving smoke ok' --require-tool-call --require-structured-output --model hfr-mock-model --out experiments/qwen3_4b_flightrecorder/serving/mock_openai_check`: pass.
- `uv run python scripts/build_serving_demo_report.py --arm baseline=experiments/qwen3_4b_flightrecorder/evaluations/baseline/evaluation_summary.json --arm trace_only=experiments/qwen3_4b_flightrecorder/evaluations/trace_only/evaluation_summary.json --arm flightrecorder=experiments/qwen3_4b_flightrecorder/evaluations/flightrecorder/evaluation_summary.json --out experiments/qwen3_4b_flightrecorder/serving/demo_run.json --report experiments/qwen3_4b_flightrecorder/serving/DEMO_REPORT.md`: pass, 5 claims.
- `uv run python -m unittest tests.test_deployment_hardening`: pass, 14 tests.
- `uv run python -m unittest discover`: fail, 318/319 tests passed; unrelated validation-layer failure remains in `tests.test_validation.ValidationTests.test_validate_warns_on_legacy_training_export_without_trainer_views` where non-strict validation returned 1 instead of 0.
- `uv run python -m flightrecorder schemas --check experiments/autonomy/supervisor_state.json`: pass.
- `pgrep -fl 'serve_transformers_openai.py|check_openai_serving.py|build_serving_demo_report.py|evaluate_hermes_heldout.py'`: pass after cleanup; no matching processes remained.

### Failures Or Blockers

- No Goal 6 blocker for preflight artifacts or replay demo generation.
- Full-suite verification is not clean because of the existing non-Goal-6 validation-layer failure noted above.
- Real baseline, trace-only, and Flight Recorder endpoint profiles were not captured because no managed lifecycle helper exists yet and unmanaged local serving should not be left running.

### Next Recommended Packet

Add a managed local server lifecycle helper that starts a serving endpoint with PID/log/profile artifacts, waits for `check_openai_serving.py` readiness, passes the profile into Eval, and guarantees process termination after the suite.

## Checkpoint: Governance Promotion Decision Artifact

### Objective

Advance Goal 8 by making promotion readiness a first-class, policy-driven artifact that blocks alias movement when required evidence is missing or unsafe.

### Work Completed

- Added `flightrecorder promotion-decision`, a side-effect-free command that combines required artifacts, eval summaries, and gate outputs into `hfr.promotion_decision.v1`.
- Added default and file-backed promotion policy handling for required artifacts, eval arms, baseline comparisons, rollback metadata, cards, license status, redaction status, safety gate status, and critical-rule blockers.
- Added validation for promotion decisions and wired `flightrecorder validate --promotion-decision`.
- Added regression tests for complete approvals and blockers for missing model cards, missing rollback metadata, unknown license, eval mismatches, new critical failures, and secret exposure.
- Added a strict demo promotion policy and Goal 8 governance documentation.
- Restored Python 3.9 CLI verification by replacing `datetime.UTC` in `flightrecorder/model_registry.py` with `timezone.utc`.

### Files Changed

- `flightrecorder/governance.py`
- `flightrecorder/cli.py`
- `flightrecorder/validation.py`
- `flightrecorder/model_registry.py`
- `tests/test_promotion_decision.py`
- `examples/promotion_policy.demo.json`
- `docs/agentic-finetune-governance.md`
- `docs/agentic-finetune-autonomous-goals.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `hfr.promotion_decision.v1` builder and validator
- `examples/promotion_policy.demo.json`
- `docs/agentic-finetune-governance.md`

### Verification Run

- `python3 -m py_compile flightrecorder/governance.py flightrecorder/cli.py flightrecorder/validation.py flightrecorder/model_registry.py tests/test_promotion_decision.py`: pass.
- `python3 -m unittest tests.test_promotion_decision tests.test_promotion_ledger tests.test_compare_gate`: pass, 26 tests.
- `python3 -m flightrecorder schemas --check experiments/autonomy/supervisor_state.json`: pass.

### Failures Or Blockers

- No Goal 8 blocker for the promotion-decision artifact packet.
- Full-suite status is not re-claimed here because unrelated concurrent layers are actively changing the tree.

### Next Recommended Packet

Add side-effect-controlled registry alias movement plans and receipts that require a validated passing promotion decision before moving `candidate`, `champion`, or `rollback` aliases.

## Checkpoint: Registry Alias Movement Receipts

### Objective

Advance Goal 8 by making registry alias movement auditable and gated before any side-effectful alias write.

### Work Completed

- Added `hfr.registry_alias_receipt.v1`, a dry-run receipt for proposed registry alias moves.
- Added `flightrecorder model-registry alias-receipt`, which reads a model registry and promotion decision, checks that promotion passed, checks registered alias targets, requires rollback for `champion`, and writes a receipt without mutating the registry.
- Added `flightrecorder validate --registry-alias-receipt`.
- Added regression tests proving passing promotion decisions create ready receipts, blocked promotion decisions only create blocked receipts, champion moves require rollback, and receipt generation does not mutate the registry.
- Documented the promotion-decision-to-alias-receipt flow.

### Files Changed

- `flightrecorder/governance.py`
- `flightrecorder/cli.py`
- `flightrecorder/validation.py`
- `tests/test_promotion_decision.py`
- `docs/agentic-finetune-governance.md`
- `docs/agentic-finetune-autonomous-goals.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `hfr.registry_alias_receipt.v1` builder and validator
- `flightrecorder model-registry alias-receipt`
- `flightrecorder validate --registry-alias-receipt`

### Verification Run

- `python3 -m py_compile flightrecorder/governance.py flightrecorder/cli.py flightrecorder/validation.py flightrecorder/model_registry.py tests/test_promotion_decision.py`: pass.
- `python3 -m unittest tests.test_promotion_decision tests.test_promotion_ledger tests.test_compare_gate`: pass, 29 tests.

### Failures Or Blockers

- No Goal 8 blocker for alias movement receipts.
- Actual registry alias mutation remains intentionally separate and should revalidate the receipt immediately before applying side effects.

### Next Recommended Packet

Add deterministic model-card and dataset-card generation helpers that summarize evidence, license, redaction, eval, rollback, and safety limits for promotion packets.

## Checkpoint: Promotion Card Generation

### Objective

Advance Goal 8 by generating deterministic model and dataset cards from the same evidence packet used by promotion decisions.

### Work Completed

- Added `hfr.promotion_cards.v1`, a manifest that records generated card content, required sections, content hashes, source artifacts, eval summaries, gates, checks, and policy.
- Added `flightrecorder promotion-cards`, which writes `MODEL_CARD.md`, `DATASET_CARD.md`, and `promotion_cards.json`.
- Added `flightrecorder validate --promotion-cards`.
- Added tests proving generated cards validate, include required sections, can be fed into `promotion-decision`, and block when rollback source metadata is missing.
- Documented the card generation and validation flow in Goal 8 governance docs.

### Files Changed

- `flightrecorder/governance.py`
- `flightrecorder/cli.py`
- `flightrecorder/validation.py`
- `tests/test_promotion_decision.py`
- `docs/agentic-finetune-governance.md`
- `docs/agentic-finetune-autonomous-goals.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `hfr.promotion_cards.v1` builder and validator
- `flightrecorder promotion-cards`
- `flightrecorder validate --promotion-cards`

### Verification Run

- `python3 -m py_compile flightrecorder/governance.py flightrecorder/cli.py flightrecorder/validation.py flightrecorder/model_registry.py tests/test_promotion_decision.py`: pass.
- `python3 -m unittest tests.test_promotion_decision tests.test_promotion_ledger tests.test_compare_gate`: pass, 31 tests.

### Failures Or Blockers

- No Goal 8 blocker for generated promotion cards.
- Release records still need to bind promotion decisions, generated cards, alias receipts, rollback targets, and release notes into one auditable promotion artifact.

### Next Recommended Packet

Add release-record artifacts that bind promotion decisions, generated cards, alias receipts, rollback targets, eval summaries, and human-readable release notes before final promotion.

## Checkpoint: Promotion Release Records

### Objective

Advance Goal 8 by binding passed governance artifacts into a side-effect-free release record before final promotion.

### Work Completed

- Added `hfr.promotion_release_record.v1`, which binds promotion decisions, promotion cards, registry alias receipts, rollback metadata, eval summaries, policy, and generated release notes.
- Added `flightrecorder promotion-release-record`.
- Added `flightrecorder validate --promotion-release-record`.
- Added tests proving release records pass when all governance components pass and block when the alias receipt is failed.
- Documented the release-record and release-note validation flow.

### Files Changed

- `flightrecorder/governance.py`
- `flightrecorder/cli.py`
- `flightrecorder/validation.py`
- `tests/test_promotion_decision.py`
- `docs/agentic-finetune-governance.md`
- `docs/agentic-finetune-autonomous-goals.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `hfr.promotion_release_record.v1` builder and validator
- `flightrecorder promotion-release-record`
- `flightrecorder validate --promotion-release-record`

### Verification Run

- `python3 -m py_compile flightrecorder/governance.py flightrecorder/cli.py flightrecorder/validation.py flightrecorder/model_registry.py tests/test_promotion_decision.py`: pass.
- `python3 -m unittest tests.test_promotion_decision tests.test_promotion_ledger tests.test_compare_gate`: pass, 33 tests.

### Failures Or Blockers

- No Goal 8 blocker for release records.
- Guarded, side-effectful alias mutation still needs to require and revalidate the release record before changing registry aliases.

### Next Recommended Packet

Add a guarded alias apply path that mutates registry aliases only after revalidating a passed alias receipt and release record against current file hashes.

## Checkpoint: Evidence Gate Decision Contracts

### Objective

Goal 1 Evidence Layer: prevent downstream layers from consuming malformed,
low-signal, missing, or unsafe evidence by making gate handoffs explicit,
actionable, and bundle-enforced.

### Work Completed

- Added a shared gate-contract helper for readiness, recommendation, failed
  checks, blocking counts, key metrics, and next actions.
- Applied the contract to suite, training, compare, and reviewed gates while
  preserving existing `passed` and check fields.
- Made evidence bundles summarize each `--gate` contract and block gates that
  are bare, malformed, internally inconsistent, or missing decision context.
- Extended strict validation to check evidence-bundle gate contracts.
- Added failed-check and next-action payloads to action, improvement, and
  promotion ledger gate decisions.
- Extended validation to reject malformed ledger-gate decision next actions.
- Kept legacy training-export validation non-strict for truly missing registry,
  redaction, and label-provenance metadata while still treating malformed
  present metadata as errors.
- Narrowed redaction scanning so structured evidence IDs such as
  `bad:secret_exposure:3` are not mistaken for leaked assignment values.
- Updated README gate-handoff documentation.

### Files Changed

- `flightrecorder/gate_contract.py`
- `flightrecorder/suite_gate.py`
- `flightrecorder/training_gate.py`
- `flightrecorder/compare_gate.py`
- `flightrecorder/reviewed_gate.py`
- `flightrecorder/action_gate.py`
- `flightrecorder/improvement_gate.py`
- `flightrecorder/promotion_gate.py`
- `flightrecorder/bundle.py`
- `flightrecorder/validation.py`
- `flightrecorder/training.py`
- `tests/test_training_gate.py`
- `tests/test_cli_report.py`
- `tests/test_compare_gate.py`
- `tests/test_reviewed_gate.py`
- `tests/test_evidence_bundle.py`
- `tests/test_validation.py`
- `tests/test_action_ledger.py`
- `tests/test_improvement_gate.py`
- `tests/test_promotion_ledger.py`
- `README.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- Unified gate decision contract on suite, training, compare, and reviewed gate
  JSON outputs.
- Evidence-bundle `metrics.gates[*].contract` summaries with blocking checks
  for weak gate artifacts.
- Ledger-gate `decision.failed_checks`, `decision.next_action_count`, and
  `decision.next_actions` payloads for action, improvement, and promotion
  ledgers.
- Fresh evidence handoff artifacts under
  `experiments/autonomy/evidence_handoff_20260702T134654Z`.

### Verification Run

- `python3.11 -m unittest tests.test_training_gate tests.test_compare_gate tests.test_reviewed_gate tests.test_cli_report tests.test_evidence_bundle`: pass, 80 tests.
- `python3.11 -m unittest tests.test_training_export tests.test_validation tests.test_schema_registry tests.test_trainer_preflight`: pass, 70 tests.
- `python3.11 -m unittest discover`: pass, 328 tests.
- Fresh `run-suite --export-rl --validate --strict --evidence-handoff`, suite
  gate, training gate, evidence bundle, strict validate, and schema check:
  pass.
- `python3.11 -m flightrecorder schemas --check experiments/autonomy/supervisor_state.json`: pass.
- `git diff --check`: pass.
- `PYTHON=python3.11 ./release_check.sh`: pass.

### Failures Or Blockers

- No Goal 1 blocker in this checkpoint.
- The system `python` command is unavailable, so release verification was run
  with `PYTHON=python3.11`.
- This worker did not commit or push changes.

### Next Recommended Packet

After the live-provider harness bridge lands, bind harness manifest/result
lineage into evidence bundles and require those contracts before downstream Eval
or Governance consumers proceed.

## Checkpoint: Model Registry License Gates And Dry-Run Plans

### Objective

Goal 4 Model Layer: add base-model candidate metadata, local registry aliases,
license/terms review gates, and dry-run training-plan artifacts without model
downloads, heavy ML imports, or GPU work.

### Work Completed

- Added dependency-free model-layer builder and validation logic for
  `model_candidate`, `model_registry_entry`, `model_registry`, and
  `training_plan` artifacts.
- Added CLI commands for candidate validation, registry validation, candidate
  registration, registry listing, alias movement, and dry-run training-plan
  generation.
- Enforced a conservative training-license allowlist:
  `approved` status, approved review, accepted terms, and
  `training_allowed == true`.
- Blocked `unknown`, `restricted`, `rejected`, `noncommercial`, and future
  non-allowlisted statuses from training selection and dry-run generation.
- Required explicit rollback targets before moving the `champion` alias.
- Added public schema contracts and catalog entries for model-layer artifacts.
- Added a no-download local mock candidate, generated registry, and generated
  dry-run training plan under `experiments/registry/`.
- Documented the model-layer registry commands and gates.

### Files Changed

- `flightrecorder/model_registry.py`
- `flightrecorder/cli.py`
- `flightrecorder/validation.py`
- `flightrecorder/schemas/model_candidate.v1.schema.json`
- `flightrecorder/schemas/model_registry_entry.v1.schema.json`
- `flightrecorder/schemas/model_registry.v1.schema.json`
- `flightrecorder/schemas/training_plan.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_model_registry.py`
- `tests/test_schema_registry.py`
- `experiments/registry/model_candidates/local_mock_tiny_chat.json`
- `experiments/registry/model_scout_manifest.json`
- `experiments/registry/datasets/mock_dataset_manifest.json`
- `experiments/registry/model_registry.json`
- `experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json`
- `docs/model-layer-registry.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `hfr.model_candidate.v1`
- `hfr.model_registry_entry.v1`
- `hfr.model_registry.v1`
- `hfr.training_plan.v1`
- `experiments/registry/model_registry.json`
- `experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json`

### Verification Run

- `python3 -m unittest tests.test_model_registry`: pass, 5 tests.
- `python3 -m flightrecorder model-candidate validate --candidate experiments/registry/model_candidates/local_mock_tiny_chat.json --require-training-eligible`: pass.
- `python3 -m flightrecorder validate --model-candidate experiments/registry/model_candidates/local_mock_tiny_chat.json --model-registry experiments/registry/model_registry.json --training-plan experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json --strict`: pass.
- `python3 -m flightrecorder schemas --check experiments/registry/model_candidates/local_mock_tiny_chat.json --name model_candidate`: pass.
- `python3 -m flightrecorder schemas --check experiments/registry/model_registry.json --name model_registry`: pass.
- `python3 -m flightrecorder schemas --check experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json --name training_plan`: pass.
- `python3 -m unittest tests.test_schema_registry`: pass, 18 tests.

### Failures Or Blockers

- No Goal 4 blocker for this packet.
- `python` and `pytest` were unavailable in this shell, so verification used
  `python3 -m unittest`.
- No real model weights were downloaded and no GPU work was launched.

### Next Recommended Packet

Add no-download compatibility probe report artifacts for tokenizer,
chat-template, serving-engine, tool-call, structured-output, context,
quantization, and memory metadata so model scouting can distinguish recorded
claims from verified probes.

## Checkpoint: Model Compatibility Report Artifacts

### Objective

Goal 4 Model Layer: convert recorded candidate compatibility metadata into a
first-class no-download report artifact that can be validated and handed to
Training, Serving, Eval, and Governance workers.

### Work Completed

- Added `hfr.model_compatibility_report.v1` builder and semantic validation.
- Added `flightrecorder model-candidate compatibility-report`.
- Added `flightrecorder validate --model-compatibility-report`.
- Added public JSON Schema and schema-catalog coverage.
- Reported all eight Goal 4 compatibility dimensions: context, tokenizer,
  chat template, serving, tool calls, structured outputs, quantization, and
  memory.
- Generated `experiments/registry/compatibility_reports/local_mock_tiny_chat.json`
  without downloads, heavy ML imports, network calls, or GPU work.
- Updated model-layer docs with the compatibility-report command.

### Files Changed

- `flightrecorder/model_registry.py`
- `flightrecorder/cli.py`
- `flightrecorder/validation.py`
- `flightrecorder/schemas/model_compatibility_report.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_model_registry.py`
- `tests/test_schema_registry.py`
- `experiments/registry/compatibility_reports/local_mock_tiny_chat.json`
- `docs/model-layer-registry.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `hfr.model_compatibility_report.v1`
- `experiments/registry/compatibility_reports/local_mock_tiny_chat.json`

### Verification Run

- `python3 -m unittest tests.test_model_registry tests.test_schema_registry`: pass, 23 tests.
- `python3 -m flightrecorder model-candidate compatibility-report --candidate experiments/registry/model_candidates/local_mock_tiny_chat.json --out experiments/registry/compatibility_reports/local_mock_tiny_chat.json`: pass.
- `python3 -m flightrecorder validate --model-candidate experiments/registry/model_candidates/local_mock_tiny_chat.json --model-compatibility-report experiments/registry/compatibility_reports/local_mock_tiny_chat.json --model-registry experiments/registry/model_registry.json --training-plan experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json --strict`: pass.
- `python3 -m flightrecorder schemas --check experiments/registry/compatibility_reports/local_mock_tiny_chat.json --name model_compatibility_report`: pass.

### Failures Or Blockers

- No Goal 4 blocker for this packet.
- The report is explicit that current probes are recorded metadata, not live
  tokenizer or serving verification.

### Next Recommended Packet

Add lifecycle-link operations for model registry entries so future workers can
attach dataset versions, training runs, adapters, eval summaries, and promotion
decisions without hand-editing registry JSON.

## Checkpoint: Model Registry Lifecycle Links

### Objective

Goal 4 Model Layer: attach dataset and training-plan references to registered
model entries through a validated command path instead of hand-editing registry
JSON.

### Work Completed

- Exercised `flightrecorder model-registry link` on the generated local model
  registry.
- Linked `mock-dataset-v1` to `local_mock_tiny_chat` under `datasets`.
- Linked the dry-run SFT plan to `local_mock_tiny_chat` under `training_runs`.
- Verified that the updated registry preserves alias state and validates after
  lifecycle links are added.
- Documented lifecycle link commands for dataset and training-run references.

### Files Changed

- `experiments/registry/model_registry.json`
- `docs/model-layer-registry.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- Updated `experiments/registry/model_registry.json` with `datasets` and
  `training_runs` lifecycle links.

### Verification Run

- `python3 -m flightrecorder model-registry link --registry experiments/registry/model_registry.json --entry candidate --type dataset --artifact-id mock-dataset-v1 --path experiments/registry/datasets/mock_dataset_manifest.json --metadata redaction_status=redacted`: pass.
- `python3 -m flightrecorder model-registry link --registry experiments/registry/model_registry.json --entry candidate --type training-run --artifact-id local_mock_tiny_chat_sft_dry_run --path experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json --metadata mode=sft --metadata dry_run=true`: pass.
- `python3 -m flightrecorder validate --model-registry experiments/registry/model_registry.json --strict`: pass.

### Failures Or Blockers

- No Goal 4 blocker for this packet.
- Adapter, eval, and promotion-decision links remain available command paths
  but were not attached because no completed adapter, eval summary, or promotion
  decision belongs to this mock candidate yet.

### Next Recommended Packet

Validate registry alias governance receipts from promotion decisions before any
future worker mutates `champion`, `candidate`, or `rollback` aliases.

### Amendment: Eval Link And Full Link Coverage

- Added a concrete `evals` lifecycle link for
  `experiments/qwen3_4b_flightrecorder/evaluations/flightrecorder/evaluation_summary.json`
  using `artifact_id: eval-flightrecorder-heldout`.
- Extended focused tests so `model-registry link` covers dataset, training-run,
  adapter, eval, and promotion-decision link categories plus helper error
  cases.
- Verified:
  - `.venv/bin/python -m unittest tests.test_model_registry tests.test_schema_registry`: pass, 24 tests.
  - `.venv/bin/python -m py_compile flightrecorder/model_registry.py flightrecorder/cli.py tests/test_model_registry.py`: pass.
  - `.venv/bin/python -m flightrecorder validate --model-registry experiments/registry/model_registry.json --strict`: pass.
  - `.venv/bin/python -m flightrecorder schemas --check experiments/registry/model_registry.json --name model_registry`: pass.

## Checkpoint: Training Registry Plans And Smoke Fixture Contracts

### Objective

Goal 5 Training Layer: make agentic LoRA training reproducible from registered
model and dataset manifests, strengthen dry-run/smoke paths before heavy ML
imports, and make the no-download smoke fixture schema-checkable.

### Work Completed

- Extended `scripts/train_agentic_lora.py` with registry-backed model and
  dataset manifest checks, action-SFT mode selection, reward/process-reward
  plan-only extension points, launch blocking before heavy imports, classified
  result archives, and JSONL registry events.
- Added `--write-smoke-fixture` to write a tiny registered fixture with local
  model manifest, dataset manifest, and trainer views for trace SFT, curated
  SFT, action SFT, DPO, reward-model, and step-reward planning.
- Preserved full prepared row counts in dry-run plans when `--limit` is used so
  row-limited smoke checks do not hide full fixture size.
- Added public schemas for training plans, training results, training registry
  events, and the new `hfr.agentic_lora_smoke_fixture.v1` fixture manifest.
- Updated focused training tests and Goal 5 docs for registry inputs, result
  archives, smoke fixtures, and schema checks.
- Merged the Training packets into `supervisor_state.json` while preserving
  concurrent model, harness, evidence, serving/demo, and governance state.

### Files Changed

- `scripts/train_agentic_lora.py`
- `flightrecorder/schemas/agentic_lora_training_plan.v1.schema.json`
- `flightrecorder/schemas/agentic_lora_training_result.v1.schema.json`
- `flightrecorder/schemas/agentic_lora_training_registry_event.v1.schema.json`
- `flightrecorder/schemas/agentic_lora_smoke_fixture.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_agentic_lora_training_plan.py`
- `docs/agentic-finetune-training-layer.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `/tmp/hfr-agentic-lora-smoke/smoke_fixture.json`
- `/tmp/hfr-agentic-lora-smoke/out/fr_sft_dpo_plan.json`
- `/tmp/hfr-agentic-lora-schema-export/agentic_lora_smoke_fixture.v1.schema.json`
- `/tmp/hfr-agentic-lora-schema-export/manifest.json`

### Verification Run

- `python3 -m unittest tests.test_agentic_lora_training_plan`: pass, 6 tests.
- `python3 -m py_compile scripts/train_agentic_lora.py tests/test_agentic_lora_training_plan.py`: pass.
- `python3 scripts/train_agentic_lora.py --write-smoke-fixture /tmp/hfr-agentic-lora-smoke`: pass.
- `python3 -m flightrecorder schemas --check /tmp/hfr-agentic-lora-smoke/smoke_fixture.json`: pass.
- `python3 scripts/train_agentic_lora.py --mode fr_sft_dpo --dry-run --require-registered-inputs --experiment-dir /tmp/hfr-agentic-lora-smoke --model-manifest /tmp/hfr-agentic-lora-smoke/registry/model_candidate.json --dataset-manifest /tmp/hfr-agentic-lora-smoke/registry/dataset_version.json --output-dir /tmp/hfr-agentic-lora-smoke/out --limit 1 --disable-trackio`: pass; 28 checks passed, `prepared_counts.sft = 1`, `prepared_counts.dpo = 1`, `full_prepared_counts.sft = 4`, `full_prepared_counts.dpo = 2`.
- `python3 -m flightrecorder schemas --check /tmp/hfr-agentic-lora-smoke/out/fr_sft_dpo_plan.json`: pass.
- `python3 -m flightrecorder schemas --name agentic_lora_smoke_fixture --write-dir /tmp/hfr-agentic-lora-schema-export --force`: pass.

### Failures Or Blockers

- No expensive training, model downloads, promotion, or unredacted-trace
  training was launched.
- This packet validates smoke/dry-run planning only; a true tiny local training
  launch still needs dependency and hardware preflight before it is safe to run.
- The shared supervisor state is being updated by multiple workers, so this
  checkpoint preserves unrelated current/next packets rather than taking over
  the global active packet.

### Next Recommended Packet

Add a bounded dependency and hardware preflight for tiny smoke launches that can
classify missing trainer dependencies or local resource blockers and write a
result archive plus registry event without downloading model weights or starting
expensive jobs.

## Checkpoint: Training Launch Dependency Preflight

### Objective

Goal 5 Training Layer: add a bounded preflight path for smoke launches that
classifies local trainer dependency blockers and writes handoff artifacts before
any model download, device allocation, or training starts.

### Work Completed

- Added `--preflight-only` to `scripts/train_agentic_lora.py`.
- The preflight path validates and writes the normal training plan first, then
  checks required trainer modules by Python module spec only.
- Preflight outcomes write the normal `<mode>_result.json` archive plus the
  JSONL registry event with explicit `preflight_passed` or
  `preflight_blocked` statuses.
- Missing local trainer packages are classified as
  `failure.category: missing_dependency` with `retryable: true`.
- The result archive records `model_downloads_started: false` and
  `training_started: false`.
- Updated training result and registry event schemas to accept preflight
  statuses.
- Added a deterministic regression test using an extra missing dependency.
- Documented launch preflight usage and safety boundaries.

### Files Changed

- `scripts/train_agentic_lora.py`
- `flightrecorder/schemas/agentic_lora_training_result.v1.schema.json`
- `flightrecorder/schemas/agentic_lora_training_registry_event.v1.schema.json`
- `tests/test_agentic_lora_training_plan.py`
- `docs/agentic-finetune-training-layer.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `/tmp/hfr-agentic-lora-preflight/out/fr_action_sft_plan.json`
- `/tmp/hfr-agentic-lora-preflight/out/fr_action_sft_result.json`
- `/tmp/hfr-agentic-lora-preflight/registry/training_events.jsonl`

### Verification Run

- `python3 -m unittest tests.test_agentic_lora_training_plan`: pass, 7 tests.
- `python3 -m py_compile scripts/train_agentic_lora.py tests/test_agentic_lora_training_plan.py`: pass.
- `python3 scripts/train_agentic_lora.py --mode fr_action_sft --preflight-only --require-registered-inputs --experiment-dir /tmp/hfr-agentic-lora-preflight --model-manifest /tmp/hfr-agentic-lora-preflight/registry/model_candidate.json --dataset-manifest /tmp/hfr-agentic-lora-preflight/registry/dataset_version.json --output-dir /tmp/hfr-agentic-lora-preflight/out --result-registry /tmp/hfr-agentic-lora-preflight/registry/training_events.jsonl --preflight-extra-dependency hfr_missing_trainer_dependency_for_test --disable-trackio`: expected exit 1 with `status: preflight_blocked`, `failure.category: missing_dependency`, no model downloads, and no training start.
- `python3 -m flightrecorder schemas --check /tmp/hfr-agentic-lora-preflight/out/fr_action_sft_result.json`: pass.
- `python3 -m flightrecorder schemas --check-jsonl /tmp/hfr-agentic-lora-preflight/registry/training_events.jsonl --name agentic_lora_training_registry_event`: pass.

### Failures Or Blockers

- No expensive training, model downloads, promotion, or unredacted-trace
  training was launched.
- The local runtime is missing several trainer dependencies (`datasets`, `peft`,
  `trl`, `transformers`, and `accelerate`), so a true tiny smoke launch remains
  blocked until dependencies are installed or an external trainer wrapper is
  used.

### Next Recommended Packet

Add side-effect-free trainer backend recipe extension artifacts for Axolotl,
LLaMA Factory, Unsloth, reward/process reward trainers, and future GRPO/RL so
external trainer wrappers can consume validated plans without changing the
registry contract.


## Checkpoint: Serving Endpoint Suite Guard

### Objective

Goal 6 Serving/Demo: add a suite-level readiness matrix so Eval and demo replay can verify required OpenAI-compatible endpoint profiles before comparing baseline and candidate behavior.

### Work Completed

- Added `scripts/verify_serving_profiles.py` to validate repeated `ARM=serving_profile.json` inputs with required arms, model identity expectations, optional adapter expectations, tool-call and structured-output requirements, strict profile-arm checks, and optional lifecycle cleanup evidence.
- Registered the public `hfr.serving_endpoint_suite.v1` schema and schema catalog entry.
- Added serving/demo regression coverage for passing endpoint suites, missing required arms, lifecycle linkage, and schema validation.
- Documented the endpoint suite guard and added it to generated experiment next commands/cards.
- Generated mock replay artifacts under `experiments/qwen3_4b_flightrecorder/serving/serving_endpoint_suite.json` and `SERVING_ENDPOINTS.md`.

### Files Changed

- `scripts/verify_serving_profiles.py`
- `flightrecorder/schemas/serving_endpoint_suite.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_serving_demo.py`
- `tests/test_schema_registry.py`
- `docs/serving-demo.md`
- `scripts/build_agentic_finetune_experiment.py`
- `experiments/qwen3_4b_flightrecorder/serving/serving_endpoint_suite.json`
- `experiments/qwen3_4b_flightrecorder/serving/SERVING_ENDPOINTS.md`
- `.omx/context/serving-demo-goal6-20260702T141018Z.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `uv run python -m unittest tests.test_serving_demo tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: pass, 9 tests.
- `uv run python -m py_compile scripts/verify_serving_profiles.py scripts/run_managed_serving_eval.py scripts/mock_openai_serving.py scripts/check_openai_serving.py scripts/build_serving_demo_report.py scripts/evaluate_hermes_heldout.py tests/test_serving_demo.py`: pass.
- `uv run python scripts/verify_serving_profiles.py ... --out experiments/qwen3_4b_flightrecorder/serving/serving_endpoint_suite.json --report experiments/qwen3_4b_flightrecorder/serving/SERVING_ENDPOINTS.md`: pass.
- `uv run python -m flightrecorder schemas --check experiments/qwen3_4b_flightrecorder/serving/serving_endpoint_suite.json`: pass.
- `uv run python -m unittest tests.test_serving_demo tests.test_deployment_hardening tests.test_schema_registry`: pass, 41 tests.
- `uv run python -m unittest discover`: pass, 343 tests.
- `pgrep -fl 'serve_transformers_openai.py|mock_openai_serving.py|run_managed_serving_eval.py|check_openai_serving.py|build_serving_demo_report.py|verify_serving_profiles.py|evaluate_hermes_heldout.py'`: pass, no matching helper processes.

### Failures Or Blockers

- No Serving/Demo blocker for this packet.
- Real baseline, trace-only, and Flight Recorder adapter endpoints still need managed capture in this environment.
- The generated endpoint suite uses mock endpoints, so it proves the guard path but not real model behavior.

### Next Recommended Packet

Capture real baseline, trace-only, and Flight Recorder endpoint profiles through `scripts/run_managed_serving_eval.py`, verify them with `scripts/verify_serving_profiles.py`, and pass the checked profiles into held-out Eval.


## Checkpoint: Local Adapter Mock Serving Suite

### Objective

Goal 6 Serving/Demo: produce a replayable three-arm serving profile suite that records real local adapter identity without launching heavyweight model servers.

### Work Completed

- Ran `scripts/run_managed_serving_eval.py` for `baseline`, `trace_only`, and `flightrecorder` against lifecycle-managed OpenAI-compatible mock endpoints.
- Bound `trace_only` to `experiments/qwen3_4b_flightrecorder/adapters/qwen3_4b_local_trace_sft/trace_sft_adapter` and `flightrecorder` to `experiments/qwen3_4b_flightrecorder/adapters/qwen3_4b_local_fr_sft_dpo/fr_sft_dpo_adapter`.
- Captured serving profiles whose adapter identities include local file hashes for adapter config/model/tokenizer/chat-template files.
- Verified a strict three-arm endpoint suite with required arms, model expectations, adapter expectations, tool-call support, structured-output support, profile-arm matching, and lifecycle cleanup evidence.

### Artifacts Produced

- `experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/baseline/serving_profile.json`
- `experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/baseline/serving_lifecycle_run.json`
- `experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/trace_only/serving_profile.json`
- `experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/trace_only/serving_lifecycle_run.json`
- `experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/flightrecorder/serving_profile.json`
- `experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/flightrecorder/serving_lifecycle_run.json`
- `experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/serving_endpoint_suite.json`
- `experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/SERVING_ENDPOINTS.md`

### Verification Run

- Managed mock capture for baseline, trace_only, and flightrecorder: pass; all preflights ready and all servers terminated.
- Strict `scripts/verify_serving_profiles.py` suite guard over the three profiles/lifecycle receipts: pass.
- `uv run python -m flightrecorder schemas --check experiments/qwen3_4b_flightrecorder/serving/local_adapter_mock_suite/serving_endpoint_suite.json`: pass.
- `uv run python -m flightrecorder schemas --check` for all three lifecycle receipts: pass.
- `pgrep -fl 'serve_transformers_openai.py|mock_openai_serving.py|run_managed_serving_eval.py|check_openai_serving.py|build_serving_demo_report.py|verify_serving_profiles.py|evaluate_hermes_heldout.py'`: pass, no matching helper processes.

### Failures Or Blockers

- No blocker for the no-download profile suite.
- This is still a mock serving suite; it verifies lifecycle, OpenAI compatibility, and adapter identity hashes, not real 4B model generation quality.

### Next Recommended Packet

Run the same managed lifecycle and suite guard against real baseline, trace-only, and Flight Recorder serving processes when local hardware/runtime capacity is available.


## Checkpoint: Live Provider Harness Bridge

### Objective

Goal 2 Harness Layer: make existing live Hermes, OpenClaw, and held-out Eval paths emit the same auditable harness manifest/result contract as mock harness runs.

### Work Completed

- Added `publish_harness_artifacts(...)` in `scripts/hermes_harness.py` for already-scored live traces.
- Added public fake-secret canary writing helper for live sandbox homes.
- Wired `scripts/live_hermes_smoke.py` to write `harness_manifest.json` and `harness_result.json` with provider/model/base URL, sandbox, canary, process, trace, scorecard, artifacts, and replay lineage.
- Wired `scripts/live_openclaw_smoke.py` to publish the same contract for OpenClaw smoke runs with isolated HOME, state, workspace, events, and gateway process metadata.
- Wired `scripts/evaluate_hermes_heldout.py` so each per-scenario Eval run publishes harness artifacts and links them from suite run rows.
- Documented the live runner bridge in `README.md`.

### Files Changed

- `scripts/hermes_harness.py`
- `scripts/live_hermes_smoke.py`
- `scripts/live_openclaw_smoke.py`
- `scripts/evaluate_hermes_heldout.py`
- `tests/test_deployment_hardening.py`
- `README.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/hermes_harness.py scripts/live_hermes_smoke.py scripts/live_openclaw_smoke.py scripts/evaluate_hermes_heldout.py tests/test_deployment_hardening.py`: pass.
- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest tests.test_deployment_hardening tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: pass, 16 tests.
- Mock harness CLI smoke with `run-scenario`, manifest/result validation, `replay-trace`, and replay-result validation: pass.
- `scripts/live_hermes_smoke.py --help`, `scripts/live_openclaw_smoke.py --help`, and `scripts/evaluate_hermes_heldout.py --help`: pass.
- Scoped `git diff --check` including the untracked harness helper file: pass.

### Failures Or Blockers

- No blocker for live-provider harness artifact publication.
- Real Hermes/OpenClaw runtime execution was not launched in this packet; help paths, unit coverage, and mock harness CLI smoke were used to avoid external runtime assumptions.

### Next Recommended Packet

Add focused Codex/Coven-style harness runner coverage for failure exits, replay receipts, evidence handoff compatibility, and docs.


## Checkpoint: Codex-Style Harness Bridge

### Objective

Goal 2 Harness Layer: harden the Codex-style Coven smoke path so detached runs publish the same harness manifest/result contract and can be tested without launching Coven.

### Work Completed

- Updated `scripts/live_coven_smoke.py` to pass real temp sandbox metadata into `publish_harness_artifacts(...)`.
- Added an isolated HOME for Coven smoke runs and wrote deterministic fake-secret canaries into it.
- Recorded Coven daemon/run/session process stdout/stderr paths and exit codes in `harness_result.json`.
- Added `harness_manifest`, `harness_result`, and `harness_runner` links to the live Coven smoke summary.
- Added a fixture-based regression that scores `fixtures/coven_detached_good.coven.jsonl`, publishes harness artifacts, and validates their public schemas without installing or launching Coven.
- Updated README Harness Runner and Live Coven docs to describe Codex-style harness artifacts.

### Files Changed

- `scripts/live_coven_smoke.py`
- `tests/test_coven_integration.py`
- `README.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/hermes_harness.py scripts/live_hermes_smoke.py scripts/live_openclaw_smoke.py scripts/live_coven_smoke.py scripts/evaluate_hermes_heldout.py tests/test_deployment_hardening.py tests/test_coven_integration.py`: pass.
- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest tests.test_deployment_hardening tests.test_coven_integration tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: pass, 20 tests.
- Fixture Coven harness generation plus `python -m flightrecorder validate --harness-manifest --harness-result --strict`: pass.
- `scripts/live_coven_smoke.py --help`: pass.
- Scoped `git diff --check` including the untracked harness helper file: pass.

### Failures Or Blockers

- No blocker for Codex/Coven-style harness artifact publication.
- A real Coven daemon was not launched in this packet; fixture and help coverage keep the smoke path testable without external runtime setup.

### Next Recommended Packet

Add cross-runner failure and replay matrix coverage for mock, Hermes-style, OpenClaw-style, and Codex-style fixture runs before Evidence consumes the artifacts.


## Checkpoint: Cross-Runner Harness Replay Matrix

### Objective

Goal 2 Harness Layer: prove fixture-backed live-style harness artifacts can replay across Hermes-style observer, OpenClaw, and Codex/Coven traces, and that failed scorecards still publish valid harness contracts.

### Work Completed

- Made live Hermes, OpenClaw, Coven, and held-out Eval artifact generation request replay-safe lineage paths.
- Added `tests/test_harness_replay_matrix.py`.
- The matrix publishes harness artifacts for Hermes-style observer rows, `fixtures/openclaw_support_ticket_good.openclaw.jsonl`, and `fixtures/coven_detached_good.coven.jsonl`.
- The matrix validates harness manifests/results, replays each `artifact_lineage.json`, and validates replay result artifacts.
- Added failed live-style scorecard coverage to confirm `harness_result.json` remains valid when scoring fails.
- Documented self-contained live replay lineage in `README.md`.

### Files Changed

- `scripts/live_hermes_smoke.py`
- `scripts/live_openclaw_smoke.py`
- `scripts/live_coven_smoke.py`
- `scripts/evaluate_hermes_heldout.py`
- `tests/test_harness_replay_matrix.py`
- `README.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m py_compile scripts/hermes_harness.py scripts/live_hermes_smoke.py scripts/live_openclaw_smoke.py scripts/live_coven_smoke.py scripts/evaluate_hermes_heldout.py tests/test_deployment_hardening.py tests/test_coven_integration.py tests/test_harness_replay_matrix.py`: pass.
- `/Users/zacharywright/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m unittest tests.test_deployment_hardening tests.test_coven_integration tests.test_harness_replay_matrix tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: pass, 22 tests.
- Replay matrix receipts for Hermes-style observer, OpenClaw JSONL, and Coven JSONL fixtures: pass.
- Scoped `git diff --check` including the untracked harness helper file: pass.

### Failures Or Blockers

- No blocker for cross-runner replay coverage.
- Real live runtimes were not launched in this packet; fixture matrix proves artifact/replay contracts without external setup.

### Next Recommended Packet

Add tool-policy enforcement matrix coverage for blocked commands, forbidden URLs, fake-secret canaries, and allowed-tool metadata across mock and fixture-backed live-style harness outputs.


## Amendment: Coven Harness Bridge Coverage

### Objective

Close the remaining live-provider harness bridge gap for Coven-style detached
stream-json smoke artifacts before moving the supervisor back to Evidence.

### Work Completed

- Confirmed the shared live harness publisher is already used by Hermes,
  OpenClaw, held-out Eval, and Coven smoke paths.
- Added deterministic deployment-hardening coverage that runs the Coven smoke
  artifact writer against the bundled Coven fixture, then schema-validates and
  semantically validates `harness_manifest.json` and `harness_result.json`.
- Updated the Harness Runner docs so Coven is explicitly listed as a live bridge
  publisher.
- Reconciled `supervisor_state.json`: the serving real-runtime capture remains
  blocked on local serving dependencies, and the active packet is now the
  unblocked Evidence handoff bridge.

### Files Changed

- `tests/test_deployment_hardening.py`
- `README.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `.venv/bin/python -m unittest tests.test_deployment_hardening tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: pass, 17 tests.
- `.venv/bin/python -m py_compile scripts/hermes_harness.py scripts/live_hermes_smoke.py scripts/live_openclaw_smoke.py scripts/live_coven_smoke.py scripts/evaluate_hermes_heldout.py tests/test_deployment_hardening.py`: pass.

### Failures Or Blockers

- No harness bridge blocker remains for artifact publication.
- Real serving runtime capture is blocked separately by missing local
  `torch`, `transformers`, `peft`, and `accelerate` dependencies; no expensive
  install, model download, or live server launch was attempted.

### Next Recommended Packet

Bind live harness manifest/result lineage into evidence handoff bundles.


## Checkpoint: Real Runtime Serving Preflight Blocker

### Objective

Goal 6 Serving/Demo: determine whether it is safe to launch real Qwen3-4B baseline and adapter endpoints in this environment.

### Result

- Local model cache exists at `~/.cache/huggingface/hub/models--Qwen--Qwen3-4B-Instruct-2507`.
- Local adapters exist for `trace_only` and `flightrecorder`.
- The current no-project serving runtime is missing `torch`, `transformers`, `peft`, and `accelerate`.
- No real model server was launched, avoiding an unmanaged heavy dependency install or partial server startup.

### Artifacts Produced

- `experiments/qwen3_4b_flightrecorder/serving/real_runtime_preflight/real_runtime_preflight.json`
- `experiments/qwen3_4b_flightrecorder/serving/real_runtime_preflight/REAL_RUNTIME_PREFLIGHT.md`

### Next Recommended Packet

Provide or activate the serving runtime dependencies, then run `scripts/run_managed_serving_eval.py` for baseline, trace_only, and flightrecorder with real `scripts/serve_transformers_openai.py` server commands and verify the resulting suite.

## Checkpoint: Training Backend Recipe Extensions

### Objective

Goal 5 Training Layer: make validated registry-backed agentic LoRA plans
consumable by external trainer wrappers for Axolotl, LLaMA Factory, Unsloth,
reward/process rewards, and future GRPO/RL without launching jobs or rewriting
the registry contract.

### Work Completed

- Added `--write-backend-recipes` to `scripts/train_agentic_lora.py`.
- The recipe path writes the normal `<mode>_plan.json` first, requires the
  source plan to pass, and requires registered model and dataset manifests.
- Added `hfr.agentic_lora_backend_recipes.v1`, a schema-checkable bundle with
  readiness, recommendation, checks, blocked reasons, recipe file refs, and an
  explicit external-runner handoff contract.
- Generated inert JSON recipe files for Axolotl, LLaMA Factory, Unsloth, and
  reward/process/RL extension points.
- Each recipe records the source training plan, selected data files,
  hyperparameters, prepared counts, input manifests, and a non-execution
  boundary.
- Updated focused tests and Training docs.

### Files Changed

- `scripts/train_agentic_lora.py`
- `flightrecorder/schemas/agentic_lora_backend_recipes.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_agentic_lora_training_plan.py`
- `docs/agentic-finetune-training-layer.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `/tmp/hfr-agentic-lora-recipes/recipes/backend_recipes.json`
- `/tmp/hfr-agentic-lora-recipes/recipes/axolotl_recipe.json`
- `/tmp/hfr-agentic-lora-recipes/recipes/llama_factory_recipe.json`
- `/tmp/hfr-agentic-lora-recipes/recipes/unsloth_recipe.json`
- `/tmp/hfr-agentic-lora-recipes/recipes/reward_process_rl_extensions_recipe.json`
- `/tmp/hfr-agentic-lora-recipes/schema-export/agentic_lora_backend_recipes.v1.schema.json`

### Verification Run

- `python3 -m unittest tests.test_agentic_lora_training_plan`: pass, 8 tests.
- `python3 -m py_compile scripts/train_agentic_lora.py tests/test_agentic_lora_training_plan.py`: pass.
- `python3 scripts/train_agentic_lora.py --mode fr_sft_dpo --write-backend-recipes /tmp/hfr-agentic-lora-recipes/recipes --require-registered-inputs --experiment-dir /tmp/hfr-agentic-lora-recipes/fixture --model-manifest /tmp/hfr-agentic-lora-recipes/fixture/registry/model_candidate.json --dataset-manifest /tmp/hfr-agentic-lora-recipes/fixture/registry/dataset_version.json --output-dir /tmp/hfr-agentic-lora-recipes/out --limit 1 --disable-trackio`: pass.
- `python3 -m flightrecorder schemas --check /tmp/hfr-agentic-lora-recipes/recipes/backend_recipes.json`: pass.
- `python3 -m flightrecorder schemas --name agentic_lora_backend_recipes --write-dir /tmp/hfr-agentic-lora-recipes/schema-export --force`: pass.

### Failures Or Blockers

- No expensive training, external trainer execution, model downloads, promotion,
  or unredacted-trace training was launched.
- The generated backend recipes are handoff artifacts only. External runners
  still need their own dependency/runtime setup and must revalidate the source
  plan before launching.

### Next Recommended Packet

Link training result archives, preflight outcomes, and adapter output refs back
into registered model entries using model-registry lifecycle links, without
moving promotion aliases.

## Checkpoint: Evidence Contract Blocker Closure

### Objective

Close the read-only architect blockers for Goal 1 evidence handoff safety.

### Work Completed

- Tightened gate contract summaries so a gate cannot be considered valid when
  `passed`, failed checks, readiness, and recommendation contradict each other.
- Added bundle regression coverage for a forged `passed: true` gate that still
  carries failed checks.
- Expanded training-export redaction scans to include export metadata as
  `key=value` pairs before manifest, metrics, and registry artifacts are
  accepted.
- Expanded training-export validation to recompute redaction over persisted
  manifest, dataset metrics, and dataset registry metadata.
- Added export-time and validation-time regressions for secret-like metadata.
- Re-ran full repository verification after the blocker fixes.

### Files Changed

- `flightrecorder/gate_contract.py`
- `flightrecorder/training.py`
- `flightrecorder/validation.py`
- `tests/test_evidence_bundle.py`
- `tests/test_training_export.py`
- `tests/test_validation.py`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `python3.11 -m unittest tests.test_evidence_bundle`: pass, 10 tests.
- `python3.11 -m unittest tests.test_training_export tests.test_validation`: pass, 39 tests.
- `python3.11 -m unittest tests.test_training_gate tests.test_compare_gate tests.test_reviewed_gate tests.test_cli_report tests.test_evidence_bundle`: pass, 81 tests.
- `python3.11 -m unittest tests.test_training_export tests.test_validation tests.test_schema_registry tests.test_trainer_preflight`: pass, 72 tests.
- `python3.11 -m unittest discover`: pass, 346 tests.
- `PYTHON=python3.11 ./release_check.sh`: pass.
- `git diff --check`: pass.
- `python3.11 -m flightrecorder schemas --check experiments/autonomy/supervisor_state.json`: pass.

### Failures Or Blockers

- No Goal 1 blocker remains from this checkpoint.
- `PYTHON=python3.11` is still required for release verification because the
  system `python` command is unavailable.

### Next Recommended Packet

With the harness live-provider manifest bridge now recorded, bind
`harness_manifest.json` and `harness_result.json` lineage into evidence bundles
and require those summaries before Eval or Governance consume live evidence.

## Checkpoint: Model Training Plan Compatibility Proofs

### Objective

Goal 4 Model Layer: require dry-run training plans to carry stronger
compatibility evidence before any future training runner can consume them.

### Work Completed

- Added optional compatibility-report proof support to dry-run training plans.
- `training-plan dry-run --compatibility-report` now validates the report,
  requires matching `candidate_id` and `model_id`, requires `passed: true`, and
  fingerprints the exact report file into the generated plan.
- Training-plan semantic validation now rejects mismatched or failed embedded
  compatibility reports and exposes report path/hash details in validation
  output.
- Updated `hfr.training_plan.v1` schema for optional `compatibility_report`
  records.
- Regenerated the local mock dry-run plan with the no-download compatibility
  report proof attached.
- Updated model-layer docs and focused regression coverage.

### Files Changed

- `flightrecorder/model_registry.py`
- `flightrecorder/cli.py`
- `flightrecorder/validation.py`
- `flightrecorder/schemas/training_plan.v1.schema.json`
- `tests/test_model_registry.py`
- `docs/model-layer-registry.md`
- `experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `python3 -m py_compile flightrecorder/model_registry.py flightrecorder/cli.py flightrecorder/validation.py tests/test_model_registry.py tests/test_schema_registry.py`: pass.
- `python3 -m unittest tests.test_model_registry tests.test_schema_registry`: pass, 25 tests.
- `python3 -m flightrecorder training-plan dry-run --registry experiments/registry/model_registry.json --model candidate --dataset-id mock-dataset-v1 --dataset-manifest experiments/registry/datasets/mock_dataset_manifest.json --compatibility-report experiments/registry/compatibility_reports/local_mock_tiny_chat.json --trainer local-test-trainer --mode sft --output-dir experiments/registry/adapters/local_mock_tiny_chat_sft --hyperparameter learning_rate=0.0001 --hyperparameter max_steps=0 --compute accelerator=none --compute dry_run=true --out experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json`: pass.
- `python3 -m flightrecorder validate --model-candidate experiments/registry/model_candidates/local_mock_tiny_chat.json --model-compatibility-report experiments/registry/compatibility_reports/local_mock_tiny_chat.json --model-registry experiments/registry/model_registry.json --training-plan experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json --strict`: pass, zero warnings.
- `python3 -m flightrecorder schemas --check experiments/registry/training_plans/local_mock_tiny_chat_sft_dry_run.json --name training_plan`: pass.
- `python3 -m flightrecorder schemas --check experiments/registry/compatibility_reports/local_mock_tiny_chat.json --name model_compatibility_report`: pass.

### Failures Or Blockers

- No model weights were downloaded, heavy ML packages imported, or GPU work
  launched.
- Compatibility evidence is still metadata-only for the local mock candidate;
  real base-model candidates need live tokenizer/serving probes before
  promotion-quality claims.

### Next Recommended Packet

Generate model-layer alias governance receipts from passing promotion decisions
or start real base-model scouting records with explicit license reviews and
compatibility probes.

## Checkpoint: External Eval Adapter Contract Stubs

### Objective

Address the remaining Goal 7 risk that BFCL, Inspect AI, lm-evaluation-harness, and SWE-bench were only mentioned in docs, not represented by a machine-readable eval artifact.

### Work Completed

- Added Ralph context, PRD, and test-spec artifacts for the continued Goal 7 persistence loop.
- Added `scripts/plan_external_eval_adapters.py`, a dependency-free planner for BFCL, Inspect AI, lm-evaluation-harness, and SWE-bench.
- Generated a fail-closed adapter plan at `experiments/qwen3_4b_flightrecorder/evaluations/external_adapters/external_eval_adapters.json`.
- Added tests proving the adapter plan defaults to `ready=false`, records blocking reasons, hashes the held-out scenario manifest, and requires identical scenario manifests for comparative claims.
- Documented the eval layer and external adapter workflow in `docs/agentic-finetune-eval-layer.md`.
- Updated the experiment README to state that external eval adapters fail closed until optional dependencies and required inputs are deliberately available.

### Files Changed

- `.omx/context/eval-layer-goal7-20260702T141955Z.md`
- `.omx/plans/prd-eval-layer-goal7.md`
- `.omx/plans/test-spec-eval-layer-goal7.md`
- `scripts/plan_external_eval_adapters.py`
- `tests/test_agentic_eval_layer.py`
- `docs/agentic-finetune-eval-layer.md`
- `experiments/qwen3_4b_flightrecorder/evaluations/external_adapters/external_eval_adapters.json`
- `experiments/qwen3_4b_flightrecorder/README.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `experiments/qwen3_4b_flightrecorder/evaluations/external_adapters/external_eval_adapters.json`
- `.omx/context/eval-layer-goal7-20260702T141955Z.md`
- `.omx/plans/prd-eval-layer-goal7.md`
- `.omx/plans/test-spec-eval-layer-goal7.md`

### Verification Run

- `.venv/bin/python scripts/plan_external_eval_adapters.py --scenario-manifest experiments/qwen3_4b_flightrecorder/heldout_scenarios.json --out experiments/qwen3_4b_flightrecorder/evaluations/external_adapters/external_eval_adapters.json`: expected fail-closed result, 4 adapters planned, 0 ready.
- `.venv/bin/python -m unittest tests.test_agentic_eval_layer`: pass, 5 tests.
- `.venv/bin/python -m py_compile scripts/plan_external_eval_adapters.py scripts/evaluate_hermes_heldout.py scripts/compare_agentic_finetune_results.py`: pass.

### Failures Or Blockers

- External adapters are intentionally blocked because optional benchmark dependencies and required inputs are not installed or supplied. This is safe: the artifact tells Governance to keep them out of promotion evidence.

### Next Recommended Packet

Persist `repair_work_items` and curriculum suggestions from failed eval comparisons as standalone artifacts beside the comparison JSON, so downstream repair/curriculum loops do not need to parse the entire comparison result.

## Checkpoint: Standalone Eval Repair And Curriculum Outputs

### Objective

Make failed eval comparison pressure consumable by downstream repair/curriculum loops without requiring them to parse the full cross-arm comparison JSON.

### Work Completed

- Extended `scripts/compare_agentic_finetune_results.py` to write `eval_repair_work_items.json` and `eval_curriculum_suggestions.json` beside the comparison output by default.
- Added `--repair-out` and `--curriculum-out` overrides for custom artifact paths.
- Added machine-readable handoff blocks for empty and non-empty repair/curriculum outputs.
- Added regression coverage proving mismatched held-out scenarios produce standalone repair and curriculum artifacts.
- Regenerated the current comparison so the experiment directory now contains the standalone handoff artifacts.
- Patched `flightrecorder validate` to tolerate absent optional parser attributes, fixing comparison-export validation after concurrent model-layer CLI additions.
- Updated eval-layer docs and the experiment README.

### Files Changed

- `scripts/compare_agentic_finetune_results.py`
- `tests/test_agentic_eval_layer.py`
- `docs/agentic-finetune-eval-layer.md`
- `experiments/qwen3_4b_flightrecorder/eval_repair_work_items.json`
- `experiments/qwen3_4b_flightrecorder/eval_curriculum_suggestions.json`
- `experiments/qwen3_4b_flightrecorder/promotion_comparison.json`
- `experiments/qwen3_4b_flightrecorder/README.md`
- `flightrecorder/cli.py`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `experiments/qwen3_4b_flightrecorder/eval_repair_work_items.json`
- `experiments/qwen3_4b_flightrecorder/eval_curriculum_suggestions.json`

### Verification Run

- `.venv/bin/python scripts/compare_agentic_finetune_results.py --baseline experiments/qwen3_4b_flightrecorder/evaluations/baseline/suite_summary.json --trace-only experiments/qwen3_4b_flightrecorder/evaluations/trace_only/suite_summary.json --flightrecorder experiments/qwen3_4b_flightrecorder/evaluations/flightrecorder/suite_summary.json --out experiments/qwen3_4b_flightrecorder/promotion_comparison.json --report experiments/qwen3_4b_flightrecorder/PROMOTION_REPORT.md`: pass.
- `.venv/bin/python -m unittest tests.test_agentic_eval_layer tests.test_compare_gate tests.test_compare_rl_export`: pass, 20 tests.
- `.venv/bin/python -m flightrecorder validate --suite-summary experiments/qwen3_4b_flightrecorder/evaluations/baseline/suite_summary.json --suite-summary experiments/qwen3_4b_flightrecorder/evaluations/trace_only/suite_summary.json --suite-summary experiments/qwen3_4b_flightrecorder/evaluations/flightrecorder/suite_summary.json --strict`: pass, 3 targets, zero warnings.
- `.venv/bin/python -m py_compile flightrecorder/cli.py scripts/plan_external_eval_adapters.py scripts/evaluate_hermes_heldout.py scripts/compare_agentic_finetune_results.py`: pass.

### Failures Or Blockers

- The current real comparison is passing, so the generated standalone repair/curriculum artifacts are empty handoffs. The test suite covers non-empty mismatched-scenario output.

### Next Recommended Packet

Expose suite-tag coverage in eval plans and summaries so Governance can see internal held-out, prompt-injection, forbidden-action, task-completion, regression, red-team, and harness-compatibility coverage directly.


## Checkpoint: Serving Runtime Preflight Contract

### Objective

Goal 6 Serving/Demo: make real runtime serving readiness repeatable and schema-checkable before launching heavyweight Qwen3-4B model servers.

### Work Completed

- Added `scripts/preflight_serving_runtime.py`, a dependency-free no-launch preflight for model cache, local adapter files, serving dependencies, server-script metadata, and managed lifecycle command refs.
- Added public `hfr.serving_runtime_preflight.v1` schema and schema catalog entry.
- Added focused tests for ready fake dependency/artifact preflights and blocked missing-dependency preflights.
- Documented the runtime preflight gate and added it to generated experiment next commands.
- Generated `experiments/qwen3_4b_flightrecorder/serving/real_runtime_preflight/serving_runtime_preflight.json` and `SERVING_RUNTIME_PREFLIGHT.md`.

### Files Changed

- `scripts/preflight_serving_runtime.py`
- `flightrecorder/schemas/serving_runtime_preflight.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_serving_demo.py`
- `tests/test_schema_registry.py`
- `docs/serving-demo.md`
- `scripts/build_agentic_finetune_experiment.py`
- `experiments/qwen3_4b_flightrecorder/serving/real_runtime_preflight/serving_runtime_preflight.json`
- `experiments/qwen3_4b_flightrecorder/serving/real_runtime_preflight/SERVING_RUNTIME_PREFLIGHT.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Verification Run

- `uv run python -m unittest tests.test_serving_demo tests.test_schema_registry.SchemaRegistryTests.test_catalog_loads_public_artifact_contracts`: pass, 11 tests.
- `uv run python scripts/preflight_serving_runtime.py ... --allow-blocked`: wrote blocked preflight artifact with model cache and adapters present, dependencies missing.
- `uv run python -m flightrecorder schemas --check experiments/qwen3_4b_flightrecorder/serving/real_runtime_preflight/serving_runtime_preflight.json`: pass.
- `uv run python -m py_compile scripts/preflight_serving_runtime.py scripts/verify_serving_profiles.py scripts/run_managed_serving_eval.py scripts/mock_openai_serving.py scripts/check_openai_serving.py scripts/build_serving_demo_report.py scripts/evaluate_hermes_heldout.py tests/test_serving_demo.py`: pass.
- `uv run python -m unittest tests.test_serving_demo tests.test_deployment_hardening tests.test_schema_registry`: pass, 44 tests.
- `pgrep -fl 'serve_transformers_openai.py|mock_openai_serving.py|run_managed_serving_eval.py|check_openai_serving.py|build_serving_demo_report.py|verify_serving_profiles.py|preflight_serving_runtime.py|evaluate_hermes_heldout.py'`: pass, no matching helper processes.

### Failures Or Blockers

- Real runtime serving capture remains blocked by missing `torch`, `transformers`, `peft`, and `accelerate` in the current repo runtime.
- Full `uv run python -m unittest discover` currently fails outside this packet with 25 Evidence-layer errors: `flightrecorder.bundle.NameError: _summarize_harness_handoff is not defined`.

### Next Recommended Packet

After the serving dependency stack is available, run `scripts/preflight_serving_runtime.py` without blocked checks, then use `scripts/run_managed_serving_eval.py` to capture real baseline, trace_only, and flightrecorder profiles and verify them with `scripts/verify_serving_profiles.py`.

## Checkpoint: Training Result Model Registry Links

### Objective

Goal 5 Training Layer: connect training result archives and adapter output refs
to registered model entries without mutating promotion aliases.

### Work Completed

- Added `--write-model-registry-link-plan` to `scripts/train_agentic_lora.py`.
- Result persistence now writes the result archive, appends the local training
  registry event, and optionally emits a model-registry lifecycle link plan.
- Link plans include exact `python3 -m flightrecorder model-registry link`
  argv for the training result and, after successful training, the final adapter
  directory.
- Link plans record `flight_recorder_mutated_registry: false`,
  `moves_aliases: false`, and runner-owned execution boundaries.
- Added a bundled schema and catalog registration for
  `hfr.agentic_lora_model_registry_link_plan.v1`.
- Extended focused tests and Training docs.

### Files Changed

- `scripts/train_agentic_lora.py`
- `flightrecorder/schemas/agentic_lora_model_registry_link_plan.v1.schema.json`
- `flightrecorder/schemas/manifest.json`
- `tests/test_agentic_lora_training_plan.py`
- `docs/agentic-finetune-training-layer.md`
- `experiments/autonomy/supervisor_state.json`
- `experiments/autonomy/journal/2026-07-02.md`

### Artifacts Produced

- `/tmp/hfr-agentic-lora-link-plan/registry/training_link_plan.json`
- `/tmp/hfr-agentic-lora-link-plan/out/fr_action_sft_result.json`
- `/tmp/hfr-agentic-lora-link-plan/schema-export/agentic_lora_model_registry_link_plan.v1.schema.json`

### Verification Run

- `python3 -m unittest tests.test_agentic_lora_training_plan`: pass, 8 tests.
- `python3 -m py_compile scripts/train_agentic_lora.py tests/test_agentic_lora_training_plan.py`: pass.
- `python3 scripts/train_agentic_lora.py --mode fr_action_sft --preflight-only --require-registered-inputs --write-model-registry-link-plan /tmp/hfr-agentic-lora-link-plan/registry/training_link_plan.json ...`: pass with expected exit 1 from missing test dependency, no training launched.
- `python3 -m flightrecorder schemas --check /tmp/hfr-agentic-lora-link-plan/registry/training_link_plan.json`: pass.
- `python3 -m flightrecorder schemas --check /tmp/hfr-agentic-lora-link-plan/out/fr_action_sft_result.json`: pass.
- `python3 -m flightrecorder schemas --name agentic_lora_model_registry_link_plan --write-dir /tmp/hfr-agentic-lora-link-plan/schema-export --force`: pass.

### Failures Or Blockers

- No expensive training, model downloads, alias movement, registry mutation, or
  unredacted-trace training was launched.
- Link plans are inert handoff artifacts. A runner must revalidate the training
  result, registry, license status, redaction gates, and current file hashes
  before applying lifecycle links.

### Next Recommended Packet

Resolve tiny smoke training runtime dependencies or route through an external
recipe wrapper, then run only a bounded `--limit` smoke launch after registered
inputs and all gates pass.
