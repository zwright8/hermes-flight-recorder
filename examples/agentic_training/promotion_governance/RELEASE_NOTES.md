# Release Notes

- Promotes `local/mock-candidate` for offline governance review after the compare gate records one candidate win and zero regressions.
- Keeps `local/mock-baseline` as the rollback target in the registry receipt.
- Records local-only release evidence; no provider call, benchmark launch, weight update, or external publication is performed by this fixture.
