{
  "id": "flight-recorder",
  "name": "Flight Recorder",
  "description": "Record OpenClaw agent, tool, model, session, and subagent hooks as Flight Recorder JSONL.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "outputDir": {
        "type": "string",
        "description": "Directory where .openclaw.jsonl event files are written."
      },
      "maxFieldChars": {
        "type": "integer",
        "minimum": 100,
        "maximum": 1000000,
        "description": "Maximum string length retained per captured field."
      }
    }
  },
  "activation": {
    "onStartup": true
  }
}
